<% flash.each do |flash_type, msg| %>
  <div class="alert alert-<%= flash_type %>" role="alert" style="margin-bottom:0px">
    <a href="#" class="close" data-dismiss="alert">×</a>
    <%= msg %>
  </div>
<% end %>  